class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int retVal(0);
        for (auto& item : nums)
        {
            retVal ^= item;
        }
        return retVal;
    }
};
